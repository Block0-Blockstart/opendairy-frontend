import { Box, Typography } from '@mui/material';

export const Rfp = () => {
  return (
    <Box pt={'50px'} display={'flex'} columnGap={'10px'} flexDirection={'column'}>
      <Typography color={'#8e8e8e'} variant={'h3'}>
        Not in this demo
      </Typography>
      <Typography color={'#8e8e8e'} variant={'h4'}>
        Use top menu to select deals in autions or offers tab
      </Typography>
    </Box>
  );
};
